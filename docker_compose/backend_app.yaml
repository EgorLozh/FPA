services:
  app:
    container_name: app
    build: ..
    command: poetry run python app/manage.py runserver 0.0.0.0:8000
    volumes:
      - ../.:/app
    ports:
      - "${API_PORT}:8000"
    depends_on:
      - db
    env_file:
      - ../.env